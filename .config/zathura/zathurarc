
# =============================
# Minimal Zathura Config (Suckless style)
# =============================

# --- General ---
set adjust-open best-fit
set smooth-scroll true
set selection-clipboard clipboard

# --- Fonts ---
set font "JetBrains Mono 9"

# --- Appearance ---
set default-bg "#1e1e2e"          # Dark background
set default-fg "#cdd6f4"          # Light text (statusbar, UI)

# Recolor PDF pages (dark mode)
set recolor true
set recolor-darkcolor "#1e1e2e"   # Page background
set recolor-lightcolor "#f2f2f2"  # PDF text (bright for readability)

# Make sure links are visible
set recolor-keephue true

# --- Statusbar ---
set statusbar-home-tilde true
set statusbar-v-padding 0
set statusbar-h-padding 2

# --- Keybindings (Vim-style) ---
map j scroll down
map k scroll up
map h scroll left
map l scroll right
map gg navigate top
map G  navigate bottom
map <C-d> scroll half-down
map <C-u> scroll half-up
map <C-f> scroll full-down
map <C-b> scroll full-up

# --- Zoom ---
map + zoom in
map - zoom out
map = zoom reset

# --- Navigation ---
map n search next
map N search previous
map / search
map ? search backwards

# --- Quit ---
map q quit
